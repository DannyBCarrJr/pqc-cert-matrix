# Private keys never commit; generators recreate the corpus
*.key
*_priv.pem

# Bouncy Castle jars (downloaded by composite tooling) and build junk
composite/lib/
*.class
__pycache__/
*.pyc

# Phase 3 python venv (brotli/zstandard); recreate per phase3/FINDINGS.md
phase3/.venv/
